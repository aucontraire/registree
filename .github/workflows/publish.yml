# Publishes to PyPI via Trusted Publishing (OIDC) — no API tokens stored.
# Inert until the trusted publisher is configured on PyPI:
#   PyPI -> project -> Publishing -> add GitHub publisher for
#   aucontraire/registree, workflow publish.yml, environment pypi.
name: publish

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Build sdist + wheel
        run: uv build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
